package tests.validation.consistentTransformation.inconsistentOrder

pipeline myPipeline {
    val data = Table.fromCsvFile("data.csv");
    val train, val test = data.splitRows(percentageInFirst = 0.7);

    val imputer = SimpleImputer(SimpleImputer.Strategy.Median).fit(train);
    val scaler = StandardScaler().fit(train);

    val trainImputed = train.transformTable(imputer);
    val _trainScaled = trainImputed.transformTable(scaler);

    // the message names each operation explicitly as 'callName (ClassName)'
    // $TEST$ warning r"Inconsistent preprocessing order: 'Test' applies \[transformTable \(StandardScaler\), transformTable \(SimpleImputer\)\] but 'Training' applies \[transformTable \(SimpleImputer\), transformTable \(StandardScaler\)\]"
    val testScaled = test.transformTable(scaler);
    val _testImputed = testScaled.transformTable(imputer);
}
